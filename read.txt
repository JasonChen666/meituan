1 
在使用标记时要考虑它本身的特殊意义，而不是仅仅只为了显示效果，显示效果可以用CSS来 

2
<br>  <img>是void元素，因为他们没有实际内容； （void表示无值）；

3
明白了什么是FTP（文件传输协议)  http://（超文本传输协议）  以及域名 网站名 等的区别；
输入根目录时，服务器自动查找该根目录下的index.html或者default.htm等默认文件；

4
file:///文件传输  本地使用 ； 

5
在自家的Web服务器里 使用相对路径，防止以后更换位置或者网站名时需要一个个修改URL，要想链接到别人的服务器则用URL

6
<pre> <time> 等标记

7
<img> 图片的各个格式包括JPG PNG GIF  

8
JPG适合复杂的照片图片，可以用PS对他进行质量的调整

9
PNG适合文本和较单调的图 LOGO等，可以对多种色彩进行透明，有PNG-16 PNG-24 PNG-8这三种大小的，比GIF可用色彩多

10
GIF适合动图 LOGO 文本 ，可以对一种色彩使用透明，可以使用的色彩较少

11
蒙版是对图片的边缘背景颜色进行改变，为了更好的贴合页面背景

12
块元素之间有间距，行内元素如果因为内容过大导致换行，则没有间距

13
alt属性可以在图片不能正常显示或者对视觉功能有问题的人浏览页面时，给到提示

14
浏览器要加载图像，需要在向服务器获得Html文件后，再次向服务器进行图片请求，如果图片有多个，则进行相应个数的请求，可能会同时进行这部操作，所以我们浏览页面时，看不出来

15
在页面中使用图片，最好把图片编辑到宽小于800px；使用width和height属性只不过是告诉浏览器要预留多少位置给图片，浏览器在加载图片时还是会把原来的图片加载一遍

16
html5是最新的标准，也是最后一个版本，有向后兼容性，增加了一些新特性和新元素以及属性，可以使用这些新特性创造一个WEB应用，用新元素创建更丰富的页面，html5也可以称之为活的HTML

17
页面开头务必写上<!doctype html> 告诉浏览器你使用的是html标准，在<head>标记中添加<meta charset="utf-8"> 以及<title>属性

18
<img>标记必须添加alt属性

19
可以通过验证工具查看你的HTML文件是否符合标准

20 @font-face向用户提供一种web字体，通过一种开放字体文件，这个文件放在WEB服务器中，后.woff后缀，表示web开放字体格式

21 
某一元素有属于三个类，有相同的属性，先看特定性，如果特定性相同，则使用CSS样式表中的最后一个

22
@font-face 中font-family是为该web字体设置一个名字，SRC指定字体所在URL，一般需要考虑到浏览器兼容性，会使用到woff，ttf等字体格式

23 
em是一个比例因子，1.2em= 120%，font-SIZE使用到%和em时是指在父元素指定的字体大小上按比例放大缩小字体

24
一般页面 body元素使用关键字来提示文字大小，small关键字相当于12px； 然后使用em和%来全局部署

25
border-radius 给边框画各种角度的角，可以用px，百分比

26
可以在link中添加media属性或者在css样式表中添加@media来进行媒体查询，max-width min-width根据浏览器的宽度变化来实行CSS中的样式变化，max-device-width min-device-width则针对用户实际屏幕大小，如手机屏幕大小

27
样式表如果有多个，并且其中有相同的属性定义，则使用最后一个

28
<div> <span> 用来放相关的结构和更底层的逻辑结构，可以帮助维护以及给元素样式

29
width属性设置的是内容区的宽度，元素的总宽度是内外总边距加总边框距加内容区宽度

30
层叠： 浏览器获得作者，用户，默认样式表（如果作者读者都没有写样式表，则先查找父元素相应规则，没有再使用浏览器默认规则）
       获得各样式表中的需要的规则，作者的>读者>默认  特殊的是读者可以采用!important，这是读者比作者的优先
       对比优先样式表中特定性，采用000算法，即id选择器+类选择器+元素选择器
       如果特定性相同，采用在样式表中最后面的

31
伪类：html中看不到:link :visited 但浏览器在使用时会提取，另外还有:focus :hover :active :firstchild :lastchild

32
设置块元素的宽度时他的内容不会延伸至浏览器整个宽度

33
text-align属性可以被块元素继承 表示所有内容居中

34
div h1 {}  div>h1 {}

35
box-sizing: border-box;
background-size: cover;
background-attachment: fixed;
background-image: -webkit-linear-gradient(#666CCC, #BC1324);